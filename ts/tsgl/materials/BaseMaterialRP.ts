import { AWasmGLPass } from '../../gl/pass/AWasmGLPass';
import { GLMesh } from '../../gl/core/data/GLMesh';

// class BaseMaterialRP extends AWasmGLPass {
//   constructor(protected mesh: GLMesh, ptr?: number, module?: EmscriptenModule) {
//     super(mesh.getGL(), module, ptr);
//   }
// }
